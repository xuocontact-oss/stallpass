# Netlify settings for Stallpass (Next.js). Netlify detects Next.js automatically.
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[functions]
  directory = "netlify/functions"
